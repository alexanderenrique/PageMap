[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "docpack"
version = "0.1.0"
description = "Convert PDFs into PageMap tile packages"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "typer>=0.12",
  "pymupdf>=1.24",
  "Pillow>=10.0",
  "jsonschema>=4.20",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
docpack = "docpack.cli:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["docpack*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
